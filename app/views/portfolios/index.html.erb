<div class="album text-muted">
  <div class="container">
		<%= source_helper("alert alert-warning") %>
		<div class="row <%= "sortable" if logged_in?(:site_admin) %>">

			<%#= link_to "Create New Item", new_portfolio_path if logged_in?(:site_admin) %>

			<!-- Here we'r manually configuring rails to work with partials because in this case we did'nt follow standard rails naming convention as in blogs, so it won't loop automatically until we specify in the foll. manner -->
			<%= render  partial: 'portfolio_item', collection: @portfolio_items %>
		</div>
	</div>
</div>